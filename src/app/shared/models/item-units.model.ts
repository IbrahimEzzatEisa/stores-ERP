import { ObjectStatus } from "src/app/shared/enums";

export class ItemUnits {

    itemGroupId: string
    itemId: string;
    unitId: number;
    unitName: number;
    factor: number;
    itemYear: number;
    status: ObjectStatus; 
}